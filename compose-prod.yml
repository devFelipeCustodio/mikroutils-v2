services:
  app:
    build:
      target: production
    ports:
      - "80:80"
    extends:
      file: compose-base.yml
      service: app
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true